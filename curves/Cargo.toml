[package]
name = "mina-curves"
version = "0.1.0"
edition = "2018"

[dependencies]
ark-ec = "0.3.0"
ark-ff = "0.3.0"

[dev-dependencies]
rand = { version = "0.7", default-features = false }
rand_xorshift = "0.2"
