// Foreign field addition (FFA) circuit implementation was removed.
// The code was interesting in terms of ideas, but it added maintenance burden
// and increased compile times without active usage.
// The implementation can be recovered from git history if needed in the future.
