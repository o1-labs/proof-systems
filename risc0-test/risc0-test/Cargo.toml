[package]
name = "risc0-test-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
kimchi = { path = "../../kimchi", default-features = false }
risc0-zkvm = { version = "3.0.5", default-features = false, features = ["std"] }
