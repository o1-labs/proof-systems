[package]
name = "ff-fft"
version = "0.1.0"
authors = [
    "Sean Bowe",
    "Alessandro Chiesa",
    "Matthew Green",
    "Ian Miers",
    "Pratyush Mishra",
    "Howard Wu"
]
description = "A library for evaluating FFTs over prime finite fields"
homepage = "https://libzexe.org"
repository = "https://github.com/o1-labs/zexe"
documentation = "https://docs.rs/ff-fft/"
keywords = ["cryptography", "finite fields", "fft"]
categories = ["cryptography"]
include = ["Cargo.toml", "src", "README.md", "LICENSE-APACHE", "LICENSE-MIT"]
license = "MIT/Apache-2.0"
edition = "2018"

[dependencies]
algebra-core = { path = "../algebra-core", default-features = false }
rand = { version = "0.7", default-features = false }

[lib]
crate-type = ["cdylib", "rlib"]

[build-dependencies]
wasm-bindgen-test = "0.3.13"

[dev-dependencies]
algebra = { path = "../algebra", default-features = false, features = [ "bn_382" ] }

[features]
default = [ "parallel" ]
std = ["algebra-core/std"]
parallel = ["std", "algebra-core/parallel"]
